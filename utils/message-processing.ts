// Importamos la función desde su ubicación actual
import { processWebMessage } from "../lib/web-chat-final"

// Re-exportamos la función para mantener la compatibilidad con las importaciones existentes
export { processWebMessage }
